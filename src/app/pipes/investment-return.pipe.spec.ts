import { InvestmentReturnPipe } from './investment-return.pipe';

describe('InvestmentReturnPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentReturnPipe();
    expect(pipe).toBeTruthy();
  });
});
